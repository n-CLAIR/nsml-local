#!/usr/bin/env python
from setuptools import setup, find_packages

setup_info = dict(
		name='nsml',
		version='0.0',
		description='empty nsml',
		install_requires=[
		],
		packages=find_packages()
)

setup(**setup_info)

